// ------------------------------------------------------------------------------
// 기능 : 특정 문자 제거
// ------------------------------------------------------------------------------
function fnValKeyRep(pPatt, pObj) {
	pObj.value = pObj.value.replace(pPatt, '');
}